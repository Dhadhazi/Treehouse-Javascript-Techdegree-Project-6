extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div


      h1 Saitama as a starting web developer

      p.lead.text-light
        | Just some projects, mostly from Treehouse Full Stack Javascript
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

   
      each project in projects
        .cell
          a(href=`project/${project.id}`)
            img.thumbnail(src=`/static/img/${project.images_urls[0]}.png`)
            h5= project.project_name